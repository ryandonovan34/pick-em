# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlmodel==0.0.22
psycopg2-binary==2.9.10
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt 4.1+ removed __about__ which breaks passlib's version detection.
bcrypt==4.0.1

# Config
pydantic-settings==2.7.0
pydantic[email]==2.10.4

# HTTP client (for Odds API + FCM calls)
httpx==0.28.1

# Task scheduler (pick reminders, odds refresh)
apscheduler==3.11.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1  # also used as the TestClient transport
